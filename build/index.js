"use strict";

// We are going to use Babel to convert this to code for all browsers
// We can use npm to run the script we have added in our package.json file
// The command to accomplish is "npm run babel"
var x = 1;
